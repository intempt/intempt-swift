.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/
dist/

# Credentials for the live contract test. NEVER committed — this repo is public.
.env.local
.env.*.local
*.pem
*.p8
*.p12

# The demo app's project file IS tracked (CI builds it). Only user state is not.
*.xcuserstate
project.xcworkspace/
